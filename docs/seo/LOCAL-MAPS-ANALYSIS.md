# Local SEO and Maps Applicability

Status: **Not applicable as a LocalBusiness audit**.

Implementation status: no LocalBusiness, RealEstateAgent, NAP, review or geo-grid markup was added. This preserves the truthful WebApplication/CollectionPage model while keeping transaction maps as product functionality.

The site is a nationwide public-data WebApplication. It does not represent a brokerage, branch, service-area business or physical office and exposes no business address, phone, opening hours, GBP profile or customer reviews. Adding LocalBusiness or RealEstateAgent schema would be misleading.

Maps capability detection: DataForSEO tools are installed, but authentication returned HTTP 401. No geo-grid was run, both because it would consume credits and because there is no target business/location to rank.

Map coordinates inside transaction data are product functionality, not NAP/GBP signals. Continue using WebApplication and CollectionPage schema.
